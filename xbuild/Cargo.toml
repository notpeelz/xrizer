[package]
name = "xbuild"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
clap = { version = "4.5.43", features = ["cargo", "derive"] }
nanoserde = { version = "0.2.0-beta", default-features = false, features = ["std", "json"]}
